import React, { useState } from 'react'

/**
 * * Profile Edit Page
 * @returns {JSX.Element}
 */
const EditProfile: React.FC = (): JSX.Element => {
  return <main className='w-full h-screen flex flex-col justify-center gap-12 items-center my-16'>Edit Profile</main>
}

export default EditProfile
